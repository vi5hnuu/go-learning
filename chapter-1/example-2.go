package main

import (
	"fmt"
)

func main() {
	firstName := "vishnu kumar"
	familyName := "vishnu kumar"
	age := 26
	peanutAllergy := false

	fmt.Printf("Person %s i.e %s aged %d has peanut allergen ? %v.", firstName, familyName, age, peanutAllergy)
}
